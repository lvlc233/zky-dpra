# 2026-01-22 数据库更新与搜索配置同步

## 目标
提交数据库更新，确保所有架构变更（特别是配置模型）已同步到数据库，并完成初始化。

## 变更范围
1.  **数据库迁移 (Database Migration)**:
    *   执行 `alembic revision --autogenerate` 生成新的迁移文件 `44275d33ab4b_update_schema_and_settings.py`。
    *   执行 `alembic upgrade head` 应用迁移。
    *   **结果**: 数据库 Schema 保持稳定，无结构性变更（Settings 变更仅涉及 JSON 字段内部结构，无需 DDL）。

2.  **后端模型 (Backend Models)**:
    *   确认 `src/service/setting/schema.py` 中的 `SearchSetting` 已对齐文档 L430-435（分页、日期、状态）。
    *   确认 `src/controller/api/search/schema.py` 中的 `SearchRequest` 已支持 `match_author` 等过滤器。

## 验证方式
1.  **环境检查**: 使用 `uv run alembic` 确保在正确的 Python 虚拟环境中执行。
2.  **迁移状态**: `upgrade head` 执行成功，无报错。
3.  **Schema 检查**: 确认 `Settings` 模型通过 `PydanticJSON` 存储于 `users` 表，符合设计预期。

## 结果
数据库已处于最新状态，搜索配置相关的后端模型已准备就绪。
